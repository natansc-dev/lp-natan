import { Story, Meta } from '@storybook/react'
import { {{pascalCase componentName}} } from '.'

export default {
  title: '{{pascalCase componentName}}',
  component: {{pascalCase componentName}}
} as Meta

export const Default: Story = () => <{{pascalCase componentName}} />
